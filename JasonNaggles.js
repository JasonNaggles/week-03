//The lawn tractor mulches leaves & and then they are bagged.
function lawnTractor (mulchLeaves, baggedLeaves) {

//The bagger the stores the mulched leaves into the bagger.
   if (bagger = baggedLeaves); {
    return bagger;
} 
}
//The lawn tractor cleaned up all the leaves in the yard.
    let cleanUp = lawnTractor("leaves", true)
    console.log(cleanUp);